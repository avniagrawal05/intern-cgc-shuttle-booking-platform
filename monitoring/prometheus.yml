global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers: []

rule_files: []

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'hms-application'
    metrics_path: '/hms/actuator/prometheus'
    static_configs:
      - targets: ['app:8080']
    scrape_interval: 5s
